import React from 'react';
import { Container } from 'react-bootstrap';

function ScoresPage() {
  return (
    <Container className="mt-5" dir="rtl">
      <h2>جدول امتیازات</h2>
      <p>این صفحه برای نمایش جدول امتیازات است (به‌زودی پیاده‌سازی می‌شود).</p>
    </Container>
  );
}

export default ScoresPage;