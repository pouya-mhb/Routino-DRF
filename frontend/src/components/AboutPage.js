import React from 'react';
import { Container } from 'react-bootstrap';

function AboutPage() {
  return (
    <Container className="mt-5" dir="rtl">
      <h2>درباره ما</h2>
      <p>این صفحه درباره روتینو است (به‌زودی پیاده‌سازی می‌شود).</p>
    </Container>
  );
}

export default AboutPage;